spring:
  application:
    name: vacation_backend
  profiles:
    active: dev
  cloud:
    config:
      fail-fast: true
  config:
    import: configserver:${CONFIG_HOST:http://176.113.82.235}:${CONFIG_PORT:8888}